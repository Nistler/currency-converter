import React from "react";

const Footer = () => {
  return (
    <section className="footer fixed-bottom text-light text-center mt-5">
      <div>Build with react, redux, react-router etc.</div>
    </section>
  );
};

export default Footer;
